// Template implementation (not compiled yet).
// Rename to FlacMetadataBridge.c after wiring include/library search paths.

#include "../include/FlacMetadataBridge.h"

// TODO: include libFLAC headers, e.g.:
// #include <FLAC/metadata.h>

int flac_read_tags(const char *file_path, FlacTagResult *out_result, char **error_message) {
    (void)file_path;
    (void)out_result;
    (void)error_message;
    // TODO: implement using libFLAC metadata API.
    return -1;
}

void flac_free_tag_result(FlacTagResult *result) {
    (void)result;
}

void flac_free_c_string(char *value) {
    (void)value;
}
